version: "3.8"

services:
  webserver:
    container_name: webserver
    build: "."
    ports:
      - 3000:3000
    volumes:
      - "./src:/app"
    env_file:
      - .env
    restart: unless-stopped